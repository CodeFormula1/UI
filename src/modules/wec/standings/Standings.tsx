import { Stack } from "@mui/material";
import React from "react";

const textStyle = {
    WebkitFontSmoothing: 'antialiased',
    color: 'white',
    fontFamily: 'Audiowide',
    fontSize: '3rem'
};


const Standings = () => {

    return (
        <Stack spacing={2} sx={textStyle}>
            Standings
        </Stack>
    );
};

export default Standings;
