import { Stack } from "@mui/material";
import React from "react";


const textStyle = {
    WebkitFontSmoothing: 'antialiased',
    color: 'white',
    fontFamily: 'Audiowide',
    fontSize: '3rem',
    backgroundColor: "#000" 
};


const Races = () => {
    return (
            <Stack spacing={12} sx={textStyle}>
                Races
            </Stack>

    );
};

export default Races;
